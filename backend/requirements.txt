# Core Framework
# fastapi bumped 0.115.0 -> 0.139.0: the old pin capped starlette <0.42,
# which left 8 starlette advisories (incl. CVE-2024-47874 High multipart DoS)
# unfixable. 0.139.0 requires starlette>=0.46.0 (no upper bound), floating it
# to a fully-patched release. Requires pydantic>=2.9.0 (satisfied below).
fastapi==0.139.0
uvicorn[standard]==0.27.0
pydantic==2.13.4
pydantic-settings==2.14.2

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Vector Search
chromadb==0.4.22

# LLM & AI
langchain>=0.2.0
langchain-groq>=0.1.0
groq>=0.13.0
langchain-google-genai>=0.1.0
langgraph>=0.2.20

# Speech
edge-tts>=6.1.13
pydub==0.25.1

# Scheduling
apscheduler==3.10.4

# Google APIs
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.116.0

# HTTP & WebRTC
httpx==0.26.0
requests==2.34.2
aiohttp==3.14.1

# PDF Generation
reportlab==4.0.9
pypdf==6.14.2

# Utilities
python-dotenv==1.2.2
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
pygments==2.20.0

# Date/Time
python-dateutil==2.8.2
pytz==2024.1

# Audio Processing
# Kept on numpy 1.x: the pinned chromadb 0.4.22 uses np.float_, removed in
# NumPy 2.0 -- numpy 2.x makes a fresh image unbuildable at import time.
numpy==1.26.4
soundfile==0.12.1

# Security floor pins (transitive deps with known advisories)
cryptography>=48.0.1
urllib3>=2.7.0
idna>=3.15
